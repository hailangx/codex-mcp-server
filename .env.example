# Environment variables for Codex MCP Server

# Path to the repository to index
REPO_PATH=/path/to/your/repository

# Database storage path (optional, defaults to ./data)
DB_PATH=./data

# OpenAI API key for embeddings (optional, will use local embeddings if not provided)
OPENAI_API_KEY=your-openai-api-key-here

# Log level (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

# Node environment
NODE_ENV=production